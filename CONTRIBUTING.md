# Contributing to devc

Thanks for your interest in contributing!

## Development

### Prerequisites

- Go 1.26.4+
- Docker
- Make

### Build

```sh
make build
```

### Test

```sh
make test
```

### Lint

```sh
make lint
```

## Releasing

Releases are automated via [GoReleaser](https://goreleaser.com/) and GitHub Actions.

### Creating a release

```sh
make release TAG=v1.2.3
```

This tags the current commit with the given semver version (e.g. `v1.2.3`) and pushes the tag. The
[release workflow](.github/workflows/release.yml) then:

1. Builds `devc` binaries for Linux and macOS (amd64 + arm64)
2. Creates a GitHub Release with archives and checksums
3. Commits an updated Homebrew formula to `HomebrewFormula/` in this repo

### Homebrew tap

The formula is auto-generated by GoReleaser and committed to the `HomebrewFormula/` directory in this repository. Users
install with:

```sh
brew tap sxwebdev/devc https://github.com/sxwebdev/devc
brew install devc
```

The release workflow only requires the automatic `GITHUB_TOKEN` — no additional secrets are needed.

## Submitting changes

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/my-change`)
3. Make your changes and add tests
4. Run `make test && make lint` to verify
5. Commit with a clear message describing what and why
6. Open a pull request

## Reporting issues

Open an issue on GitHub with:

- What you expected to happen
- What actually happened
- Steps to reproduce
- `devc` version and OS
